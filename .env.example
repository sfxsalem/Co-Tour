# Bound native parallelism for predictable local/container resource usage.
LOKY_MAX_CPU_COUNT=2
# Set this to the public host name when deploying behind a TLS reverse proxy.
FASTAPI_ALLOWED_HOSTS=localhost,127.0.0.1,[::1]
