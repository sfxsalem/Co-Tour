services:
  web:
    build: ./web_app
    restart: unless-stopped
    ports:
      - "127.0.0.1:8000:8000"
    environment:
      LOKY_MAX_CPU_COUNT: "${LOKY_MAX_CPU_COUNT:-2}"
      FASTAPI_ALLOWED_HOSTS: "${FASTAPI_ALLOWED_HOSTS:-localhost,127.0.0.1,[::1]}"
